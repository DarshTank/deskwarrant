# DeskWarrant host agent - Windows only, Python 3.11+
# Install:  py -3 -m pip install -r requirements.txt

# --- control plane ---
httpx==0.28.1
keyring==25.6.0

# --- system inspection ---
psutil==6.1.1
pywin32==308
uiautomation==2.0.20

# --- screen capture and encoding ---
mss==10.0.0
Pillow==11.1.0
xxhash==3.5.0

# --- data plane ---
# The live-view server, bound to 127.0.0.1 and reached through Cloudflare
# Tunnel. Pure Python: no native build step, and far lighter than the aiortc
# stack it replaced (av, pylibsrtp, cryptography).
aiohttp==3.11.11

# --- volume control (get_volume, set_volume, set_mute) ---
# pycaw drives the Windows Core Audio API; comtypes is its COM binding and is
# also used by uiautomation.
pycaw==20240210
comtypes==1.4.8

# --- optional: system tray icon (agent runs fine without it) ---
pystray==0.19.5

# --- packaging (Stage 8) ---
pyinstaller==6.11.1
