# --- secrets ---
.env
.env.local
.env*.local
!.env.example

# --- node / next ---
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# --- python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# --- pyinstaller / installer ---
agent/build/
agent/dist/
agent/Output/
# Bundled into the exe at build time; ~30 MB, fetched by the release workflow.
agent/cloudflared.exe
# Generated per build: holds the console URL baked into the binary.
agent/_build_config.py

# --- agent runtime state (also lives in %LOCALAPPDATA%, never in the repo) ---
agent/config.json
agent/*.log

# --- editors / os ---
.vscode/
.idea/
Thumbs.db
Desktop.ini
*.swp

# --- dev server capture (see CHECKPOINTS.md) ---
console/dev-*.log
